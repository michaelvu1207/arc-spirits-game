# P30 Final Pre-Authorization Review

I read the plan and all fifteen enforcement-closure files it names, plus the hash-bound `confirmation-gates.proposed.json`, the generation-audit module and its registered tests (for the realized-dose claim), and the crypto/custody primitives. Findings by focus area:

**Quantitative confirmation gates.** `confirmation-gates.proposed.json` is frozen, registered in the source registry, and matches every Phase 5 number in the plan exactly: 8,192 fresh public seeds with ≥6,631 round-30 wins (Wilson ≥0.80), ≥7,431 round-35 wins (≥0.90), 100,000-draw paired bootstrap ≥ +0.05 at the Bonferroni-corrected confidence; 4,096 private seeds with 3,337/3,732 floors and 1,024-game slice floors (≥751 wins per named slice); Elo lower bound > +50 overall, ≥0 per format, seat spread ≤0.02; exploitability +0.03/−0.01/0.40/−0.02; fairness zeros; latency/GPU ratios; Michael 28/40 with ties as non-wins and a separate pre-game Fable freeze. The analyzer-side screen (Holm across six tests, 262,144 exact sign assignments, +0.03 point floor, 13/18 and ≤2/18 replicate gates, deterministic tie-break, power-bound recomputation) is covered by the named analyzer tests, including determinism of the double-run, guardian-worst-case regression, and fail-closed stall/latency/malformed gates.

**Divergence classification.** Only `PRE_CHILD` and `RECEIPT_ONLY` are recoverable; the scheduler cross-checks the frozen `recoveryPolicy.eligibleKinds` against code constants. A consumed token without a canonical draft, an invalid draft that entered `Popen`, a second failure of the same logical action, or a seal-attempt marker without its artifact all raise terminal closure. A CUDA primary/replay mismatch raises inside a started child, which produces an invalid draft and is therefore terminal, matching "integrity failure, not retryable infrastructure." The guardian independently recomputes exact per-game and replay-hash equality in `_attest_phase0_readiness`. Training-quality failure cannot be relabelled: `RECEIPT_ONLY` requires a `valid: true` draft and `PRE_CHILD` requires proof no child started.

**Realized-dose telemetry.** Confirmed in the core audit's `validate_reach30_credit`: it requires `soloReach30Applied`, `soloReach30RealizedDose`, and per-band applied/dose maps for exactly rounds 1-8, 9-18, 19-30, rejects zero applied rows under a treatment schedule, rejects realized dose inconsistent with the frozen schedule, and forbids mixing scalar and banded credit — all with registered tests. These quantities are sealed in the audit; the scheduler's budget guard reads only receipt bytes and durations, so they cannot affect scheduling or selection.

**Duration/storage go-no-go.** The guardian preflight computes the 25%-buffered projection plus the 512 MiB evaluation reserve and refuses to sign unless the 6 GiB floor and the protocol-frozen compute ceiling both hold; `require_preflight` re-validates non-discretionarily on every scheduler call. The rolling guard recomputes from the larger of the frozen estimate and the observed per-generation maximum, maintains a real `posix_fallocate` reservation (sparse regression detected via `st_blocks`), and releases exactly one action's allocation at a time. The eleven-case fault matrix includes the real-block reserve/shrink/release proof.

**Local-only review provenance.** The keyless scheduler only emits a review-required status; the local launcher fetches hash-bound, metric-free inputs into a 0700 capsule, consumes the remote attempt marker with `O_EXCL` before invoking Fable, runs the protocol-pinned Claude executable (path, sha256, version all validated) under a default-deny sandbox with `Read` as the only tool, requires exit 0, empty stderr, and an exact final-line `VERDICT: ACCEPT`, and signs the receipt with the review-attester key held only in local custody (piped from the vault, never sent to SimForge). The receipt validator pins argv shape, environment-key allowlist, request/attempt bindings, and re-derives the verdict from stdout bytes. The analysis-phase review adds draft/manifest binding, one-shot attempt consumption, and attester-signature verification before the analysis authorization.

**One-shot launch/signing boundaries.** Executor launches require a locally signed, process-identity-bound (pid/startTicks/bootId) launch permit persisted `O_EXCL` and validated — together with the full source-lock closure — before token consumption or `Popen`; direct CLIs are disabled in the executor, issuers, and analysis bundle. Every non-executor signing action writes a durable `O_EXCL` attempt marker binding the exact unsigned plan before any key exposure, and a marker without its artifact is terminal. Executor sealing commits the normal-seal-attempt marker before the key is touched; the PRE_CHILD attempt marker precedes `Popen` and makes any crash terminal. The analysis path adds the sealed-memfd capability, launch intent/evidence, namespace child proof, and deliberately no recovery. The analysis authorization rebinding validator proves byte-identity outside the three TTL fields with a strictly post-review 24-hour window.

**Exact-stack determinism.** The preflight runs the protocol's full 4,096 common public games at production worker count and batch shapes through two fresh servers with distinct socket inodes on the pinned physical GPU7 UUID, with the frozen determinism environment (TF32 off, `CUBLAS_WORKSPACE_CONFIG`, `PYTHONHASHSEED=0`), and requires exact per-game and replay-hash equality, verified again by the guardian. Per-generation environment manifests pin the same determinism contract. The venv-launcher rule is implemented correctly: `venv_python_entrypoint` never dereferences the launcher, while `executable_sha256` resolves only to hash the interpreter bytes.

**Incident amendment.** The `e6d754a9` root cause is concretely fixed: the CUDA child now accepts a supervisor-precreated attempt directory only if it is a plain empty directory, with regression tests for the symlink, non-directory, and nonempty cases, and the supervisor's `_prepare_output_parents` behavior is unchanged. Fresh-instance repetition is structurally enforced because every Phase 0 receipt lives under the campaign-instance ledger namespace.

**Non-blocking observations (P2).** The plan's "directly reviewable enforcement closure" list names entry points but omits some load-bearing imported modules (`run_v35_p30_analysis_review_local.py`, `v35_p30_recovery.py`, `v35_p30_key_custody.py`, `run_v35_p30_evaluation_attempt.py`, `audit_v35_p30_generation.py`); all are in the source registry and required by the executor's source-lock closure check, so they are frozen and reviewable — this is a wording gap, not an enforcement gap. The 1,209,600-second ceiling is enforced against the hash-frozen protocol's `runtime.maxCampaignComputeSeconds` rather than a code literal, which is consistent with the freeze model. The gate-review sandbox may carry `ANTHROPIC_API_KEY`, which the plan explicitly accepts as the sole network-facing dependency of the review.

All prior findings named in the review request are concretely addressed in the source-locked implementation with test coverage. No P0 or P1 launch blocker remains.

VERDICT: ACCEPT
