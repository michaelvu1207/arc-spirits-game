No Write tool is available in this session, so I'll skip the memory note and deliver the final review.

---

# Final pre-authorization review — V35 P30 long-horizon campaign

**Outcome: no P0/P1 launch blocker remains.** Every prior review finding I could trace (v12, v13, v15, v17, and the aa80b50e Phase-0 rejection) has either a concrete implementation in the source-locked closure or an explicit, reasoned risk-acceptance frozen into the plan with a stop rule. I reviewed the plan, the frozen protocol and confirmation-gates artifacts, and the named enforcement files: the analyzer (~3,000 of 4,206 lines including all validation and result-assembly paths), the full scheduler, the Phase-0 gate module, the CUDA determinism preflight, the executor primitive's authorization/permit layer, the local gate-review launcher, the fault-injection matrix, and the cumulative-spend artifact.

## Verification by requested area

**Quantitative confirmation gates.** `confirmation-gates.proposed.json` is hash-pinned in the protocol (`a3aca14a…`) and its numbers match the plan's Phase 5 exactly: 6,631/8,192 round-30 wins and 7,431 round-35 wins with the Bonferroni one-sided z, the 100,000-draw paired-bootstrap +0.05 floor, 3,337/3,732 private-seed floors with 751-win 1,024-game slices, Elo +50 lower bound with per-format nonnegativity and 0.02 seat spread, the four exploitability bounds, zero-tolerance fairness counts, the latency/GPU ratios, and the 28/40 Michael gate with ties as non-wins. `validate_protocol` re-verifies the declaration and the artifact hash at every scheduler and analyzer entry.

**Divergence classification.** Three distinct classes are code-enforced: (1) primary/replay divergence — the guardian pair receipt requires exact per-game and replay-hash equality, and the analyzer independently re-checks `replay_indexed == primary_indexed`; no receipt can exist for a mismatch. (2) Infrastructure recovery — `ensure_recovery_execution_draft` admits only objectively provable PRE_CHILD (valid draft proving no Popen/seed use) or RECEIPT_ONLY (complete draft plus durable normal-seal marker); anything else, including an invalid draft after child start, raises "terminally closed." A second attempt of the same logical action is blocked by durable attempt markers. (3) Training-quality failure is never relabelled: a completed run seals a valid receipt regardless of play quality, and a failed trainer has no recovery class.

**Realized-dose telemetry.** `validate_generation_dose_telemetry` enforces, for all eight generations of all 54 endpoints, band-resolved (rounds 1–8/9–18/19–30) applied counts and doses that must reconcile arithmetically with the arm's frozen coefficients, with control arms required to show exactly zero applied credit. The analysis output pre-registers a `realizedDose` block per treatment and replicate — the v17 dose-confound reporting request is implemented, and the telemetry is decision-inert as required.

**Duration/storage go-no-go.** The guardian preflight receipt embeds the 6 GiB floor, 512 MiB evaluation reserve, and the 1,209,600 s ceiling, and fails closed if the projection exceeds either. `_rolling_budget_guard` runs before every post-authorization token: it recomputes remaining bytes/runtime from the larger of the frozen projection and the observed signed maxima, maintains a real `posix_fallocate` reservation (non-sparse verified via `st_blocks`, one-action release, zeroed only after all 54 pair receipts), and stops issuance when accumulated-plus-projected compute exceeds the ceiling — the v17 mid-campaign-breach gap is closed in code.

**Local-only review provenance.** The review-attester key is protocol-pinned local-only; the launcher signs via a 1Password pipe into a child process, so the key never reaches SimForge. The v12 absent-target fetch, v13 canonical request path (`ledger/requests/<mode>-fable-review.json` bound identically by scheduler status, launcher, and remote validator), and v15 fixes (authenticated liveness plus clock probe before the remote `O_EXCL` reservation; durable postprocess marker permitting only byte-identical signing/upload resume and never a second Fable call) are all present. `validate_gate_review_receipt` rebinds the launcher SHA-256, pinned Claude executable, digest-pinned container config, exact prompt/argv, empty stderr, and exact `VERDICT: ACCEPT`.

**One-shot launch/signing boundaries.** The executor validates every registered source hash and requires a launch permit whose action ID is the request hash, reserved via exclusive write and bound to the live executor process identity (PID, start ticks, boot ID) — replayed or stale permits fail. The analysis launcher passes a fully sealed anonymous memfd capability that the analyzer verifies (all four seals, single descriptor, hash match) inside distinct PID/user/network namespaces, making direct CLI invocation impossible even with copied ledger files. Non-executor signing is one-shot via durable signing-attempt markers checked in `emit_request`, and the durability-before-key-use property is exercised by the fault matrix.

**Exact-stack determinism.** The CUDA preflight runs the byte-identical production evaluator argv at production concurrency (4,096 games, 24 workers) against two fresh servers with distinct socket inodes under a pinned deterministic environment (TF32 disabled, cuBLAS workspace pinned, PCI_BUS_ID order, GPU-UUID-scoped visibility), requires exact per-game and replay-hash equality, and implements the corrected empty-directory and short-socket contracts from the closed instances.

## Prior v17 findings — dispositions

Findings 5 (mid-campaign ceiling) and 6 (cross-instance stopping) are implemented/frozen; the dose-reporting note is implemented. Findings 1 and 2 (all-or-nothing over 541 actions, NaN policy) are explicitly declined with a defensible estimand-preservation argument, and the plan now obligates the second Fable review to treat generation-one runtime/failure evidence and completion risk as a go/no-go input. Finding 3 is partially adopted (v2 end-to-end analyzer rehearsal, mandatory disposable host integration run, matched gen-1 stage before any 54-root authorization). Finding 4 (one-shot Phase-4 review) is retained by documented decision with liveness and resumable-postprocess mitigations. These acceptances put economic completion risk, not evidence validity, at stake, so they are not launch blockers.

## Non-blocking residuals (P2/P3)

The cross-instance budget and "no automatic eighth attempt" rule are plan-level policy — the scheduler does not read the cumulative-spend artifact (which I verified matches the plan's totals exactly). Fault-matrix cases commit but do not require empty stderr, unlike the rehearsal's stricter standard (unittest writes to stderr by design). The v17 Michael-gate power statement and document-restructuring suggestions remain unaddressed. I verified issuer-side artifacts (`issue_v35_p30_*`, role wrapper, tests, registry generator) only through their consumers' independent revalidation, which fails closed; and file-hash integrity itself is proven at runtime by the source-lock/Git verifier rather than by this review.

VERDICT: ACCEPT
