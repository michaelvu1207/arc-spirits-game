I don't have a file-writing tool in this session, so I'll deliver the review directly. Here is the final pre-authorization review.

---

# P30 final pre-authorization review (v21)

**Outcome: no P0/P1 launch blocker remains.** I reviewed `plan.md` in full plus all fifteen files in the directly reviewable enforcement closure it names, and cross-checked the frozen statistical artifacts (`confirmation-gates.proposed.json`, `calibrate_v35_p30_power.py`, `v35_p30_statistics.py`) that the closure exercises. Every prior review finding (v12, v13, v15, the aa80b50e rehearsal rejection, and the six launch incidents) is concretely addressed in code with matching regression tests, not just described in prose.

## Verification by required focus area

**Quantitative confirmation gates.** `confirmation-gates.proposed.json` is frozen, machine-readable, and matches every Phase-5 number in the plan exactly: 8,192 fresh public games with 6,631/7,431 Wilson win floors and the +0.05 paired-bootstrap bound (100,000 draws, seed 35153501); 3,337/3,732 private floors with 751-win 1,024-game slice floors; Elo +50 overall / ≥0 per-format / ≤0.02 seat spread; exploitability +0.03/−0.01/0.40/−0.02; zero-tolerance fairness and systems gates with the 100 ms / 1.10× / 60 s / 1.25× / 1.10× ratios; the 28/40 Michael gate with ties as non-wins; and an explicitly conjunctive decision rule. The efficacy screen's constants (planning effects +0.04/+0.025/+0.10, 262,144 exact sign assignments, Holm across six tests, +0.03 win-effect floor, ≥13/18 positive, ≤2/18 below −0.01, joint-power requirement 0.80 with frozen seed) are frozen in `calibrate_v35_p30_power.py` and enforced/tested in `test_analyze_v35_p30_long_horizon.py`, including conservative tie handling, guardian worst-case, stall/latency/malformed fail-closure, deterministic tie-break, and `promotionEligible: false` on every output. The sensitivity curves carry `powerClaimed: false` as the plan requires.

**Divergence classification.** `run_v35_p30_cuda_determinism.py` raises on any per-game or replay-hash inequality inside the authorized child, so a mismatch can only produce a nonzero-exit invalid draft — ineligible for `RECEIPT_ONLY` (requires `valid: true`) and for `PRE_CHILD` (child started) — which terminally closes the instance rather than permitting an infrastructure retry. The guardian independently re-reads both raw reports and recomputes exact equality plus the sealed commitments in `_attest_phase0_readiness`, exposing only commitments and fixed diagnostic codes. Deterministic replay mismatch is also enumerated as permanently closing in the frozen recovery policy.

**Realized-dose telemetry.** The generation audit's `soloReach30Credit` block records applied rows and realized coefficient dose overall and in the 1-8, 9-18, and 19-30 bands; the analyzer surfaces only verified telemetry (`_verifiedReach30DoseTelemetry` → `comparisons[arm]["realizedDose"]`), with a dedicated test. The scheduler's rolling guard reads only bytes and durations from receipts, so the sealed manipulation-check quantities cannot influence scheduling, and selection in `analyze_indexed` is driven solely by the frozen effect gates.

**Duration/storage go-no-go.** The guardian preflight computes validity non-discretionarily and raises before signing if `projectedCampaignComputeSeconds` exceeds the frozen ceiling or projected headroom falls below the 6 GiB floor plus 512 MiB evaluation reserve; `require_preflight` re-validates all of this plus current headroom on every scheduler call. The rolling guard recomputes remaining storage/runtime from the larger of the frozen estimate and the running signed maxima, maintains a real `posix_fallocate` reservation (sparse-detection, exclusive-lock, fsync), releases exactly one action's allocation before each launch, returns the reservation to zero after the pair phase, and stops issuing tokens once accumulated-plus-projected compute exceeds the ceiling — the fail-closed mid-execution semantics the plan specifies. The eleven-case fault matrix includes the real-block reservation proof.

**Local-only review provenance.** The review-attester is pinned as a local-only role; its key is read from 1Password custody into a pipe and never delivered to SimForge. The v12 fetcher fix (absent target, scp-created, regular-file/hash verification, then 0400 chmod and file+parent fsync) and the v13 canonical `ledger/requests/<mode>-fable-review.json` binding are both implemented and regression-tested, including the integration test proving the scheduler status and validator bind the same request bytes. Receipts bind the launcher's SHA-256, which the remote validator recomputes against the source lock, and pin the exact Claude argv, digest-pinned container config, fd-delivered OAuth, pre-reservation authenticated liveness and ≤30 s clock probes, remote `O_EXCL` attempt reservation, verified container deletion, empty stderr, and exact `VERDICT: ACCEPT` last line. The v15 postprocess marker permits only hash-identical signing/upload resume and can never rerun Fable.

**One-shot launch/signing boundaries.** Executor launches require a process-identity-bound signed launch permit validated (with full source-closure re-verification) before token consumption or `Popen`; direct CLI entrypoints are disabled in the executor, issuers, and analysis bundle. Non-executor signing writes a durable `O_EXCL` attempt marker and unsigned signing plan before any key exposure, with terminal closure on any retry; executor sealing commits its fsync-backed seal-attempt marker before touching the private key; `SIGN_READY` for executors is emitted only after the candidate has exited and the immutable unsigned draft validates. Analysis is strictly one-shot with no recovery path, the sealed-memfd `--ro-bind-data` capability transport, launch intent/evidence with distinct PID/user/network namespace proof, and the corrected `PRE_CHILD` semantics that treat entering `Popen` (not merely a returned handle) as the irreversible boundary. The v2 analyzer rehearsal now executes the unmodified production analyzer end-to-end through this exact path with ephemeral keys unlinked before launch, empty streams, and one-shot re-entry proven; v1/testId evidence fails closed.

**Exact-stack determinism.** The CUDA preflight runs the exact production evaluator and inference-server commands (same checkpoint, catalog, batch window/size, worker concurrency, TF32-disabled deterministic environment, hash-pinned bubblewrap and venv interpreter whose launcher path is preserved while its resolved bytes are hashed) on physical GPU7 with two fresh servers and distinct socket inodes, requiring bitwise per-game and replay-hash equality. The socket root is derived from the full campaign ID under `/dev/shm/p/` and both pathnames are validated against the 107-byte limit at authorization time, with the namespace/length contract frozen by test. The corrected attempt-directory contract (plain empty supervisor-created directory; symlink/non-directory/nonempty rejected) and the `/proc/driver/nvidia` nested-controller exclusion are implemented on both the issuing and executing sides with regression tests.

## Non-blocking observations (P2/P3)

1. `run_v35_p30_fault_injection.py` reports `platform: "linux"` for any POSIX host, so a macOS dry run would mislabel itself; identity actually rests on the executor receipt chain, so this is cosmetic (P3).
2. The pinned Docker daemon identity records the daemon-level `seccomp,profile=unconfined` default; isolation intentionally rests on the per-container `builtin-enforced` seccomp contract confirmed by the lifecycle smoke, per the accepted v15 disposition (P2, accepted).
3. The pre-production disposable host probe of the corrected `--ro-bind-data` transport remains a procedural gate ("a production campaign ID cannot exist until that probe passes") rather than a code-enforced one; the outcome-free smoke evidence and the mandatory full in-instance Phase-0 rerun bound the residual risk (P2, accepted by the seventh-pass protocol).

None of these permits outcome exposure, seed reuse, retry of a consumed one-shot action, or an unbudgeted launch.

VERDICT: ACCEPT
