I've completed the verification pass across all charged areas at their current enforcement sites. Final review follows.

---

# Final pre-authorization review (v5): V35 P30 long-horizon campaign

**Scope:** the current working-tree bytes of `plan.md` and the enforcement closure it names, read at their enforcement sites: `run_v35_p30_campaign.py` (all 2,403 lines), `run_v35_p30_role.py` (all 1,924 lines), `v35_p30_authorized_execution.py` (launch/permit/consumption boundary), `v35_p30_crypto.py` (all 410 lines), `v35_p30_phase0.py` (all 183 lines), `run_v35_p30_gate_review_local.py` (all 463 lines), `run_v35_p30_cuda_determinism.py` (all 325 lines), `confirmation-gates.proposed.json` (complete), and `analyze_v35_p30_long_horizon.py` at its frozen-contract, audit-gate, pair-integrity, and analysis-provenance regions. Several of these files changed after the v4 ACCEPT (2026-07-15), so each charged finding was re-verified against current bytes rather than carried forward; `audit_v32_generation.py` and `v35_p30_analysis_review.py` are unmodified since v4's line-level verification, which therefore stands for them.

## Verification of the prior review findings

**1. Quantitative confirmation gates — confirmed.** `confirmation-gates.proposed.json` freezes every Phase 5 number the plan summarizes, and they agree exactly: 8,192 fresh public games with Bonferroni one-sided z = 2.128045…, 6,631 round-30 wins (Wilson ≥ 0.80) and 7,431 round-35 wins (≥ 0.90), the 100,000-draw paired bootstrap (seed 35153501) with the +0.05 post-round-15 VP floor; 4,096 private games with 3,337/3,732 floors and 751-win minimums per 1,024-game slice across the four named late-state slices; +50 overall Elo lower bound with per-format nonnegativity and 0.02 seat spread; the 0.40 exploit ceiling and −0.02 improvement bound; zero-tolerance fairness and systems counters with the 100 ms/1.10× and 60 s/1.25× latency and 1.10× GPU-memory ratios; 28/40 versus Michael with ties as non-wins; and an explicit conjunctive decision rule. `validate_protocol` pins the file by SHA-256 (`a3aca14a…`) and re-verifies its on-disk hash (`analyze_v35_p30_long_horizon.py:714-727`), so any post-freeze edit fails closed.

**2. Divergence classification — confirmed.** The bounded recovery policy is byte-pinned as a frozen dict (`PRE_CHILD`/`RECEIPT_ONLY` only, one recovery per logical action, guardian classification required, second failure and outcome exposure both close the campaign; `analyze_v35_p30_long_horizon.py:630-642`). The scheduler's recovery state machine (`ensure_recovery_execution_draft`, `validate_recovery_state`, `next_execution_action`) admits exactly those two classes, requires guardian-signed incident and link attestations with `attemptOrdinal` fixed at 1, and terminally closes on a consumed token without a canonical draft, an invalid draft that entered `Popen`, or a repeated seal attempt. Deterministic replay divergence has no recovery class anywhere: the guardian pair receipt must carry exactly `["EXACT_PER_GAME_MATCH", "EXACT_REPLAY_HASH_MATCH"]` with per-game and replay-hash arrays recomputed and hash-bound (`:2776-2823`), and training-quality failure cannot be relabelled because the audit trust gates (KL, clip fractions, calibration, zero stalls/malformed rows) are deterministic schema checks, so a weak-but-valid endpoint stays a valid bad endpoint.

**3. Realized-dose telemetry — confirmed.** The analyzer re-validates each generation audit's `soloReach30Credit` block against the frozen arm contract — coefficient equality, applied-row counts strictly positive for treatments and exactly zero for control, and the frozen band schedule (`:1935-1951`) — while the per-band applied/dose arithmetic (bands 1-8, 9-18, 19-30 summing to totals, dose = applied × frozen coefficient, no simultaneous scalar+scheduled dosing) is enforced in `audit_v32_generation.validate_reach30_credit`, unchanged since its v4 line-level verification. These quantities live only in sealed audits; the scheduler never parses them, matching the plan's manipulation-check-only framing.

**4. Duration/storage go-no-go — confirmed.** `DISK_FLOOR_BYTES` (6 GiB) and `EVALUATION_RESERVE_BYTES` (512 MiB) are frozen constants (`run_v35_p30_campaign.py:79-80`); the reservation is a real `posix_fallocate` allocation with sparse-regression detection via `st_blocks` (`:115-167`), exclusive-locked, permission-checked, fsynced, and lowered one action at a time. The guardian preflight computes the 25%-buffered projection and signs `valid: True` only if the disk floor and the byte-pinned 1,209,600-second ceiling both hold, raising before signing otherwise (`run_v35_p30_role.py:1271-1300`); `require_preflight` re-checks those exact fields and current headroom on every scheduler call, and `_rolling_budget_guard` recomputes remaining storage and compute from the larger of the frozen preflight estimate and the running maximum observed per-generation bytes/runtime, stopping non-discretionarily (`:1841-1923`). The preflight start additionally hard-requires 8 GiB initial headroom.

**5. Local-only review provenance — confirmed.** All three review lanes are launcher-local and attester-signed. The gate-review launcher pulls only hash bindings from the remote scheduler status, downloads them into a fresh 0700 capsule with per-file hash verification, reserves a one-shot remote `O_EXCL` attempt before Fable runs, executes the resolved Claude binary under a sandbox profile with `Read` as the only tool, requires the last nonempty stdout line to be exactly `VERDICT: ACCEPT` with empty stderr, and signs the receipt through a local vault pipe (`op-michaelagents` → `--sign-only` child over an anonymous pipe) so the review-attester key never reaches SimForge (`run_v35_p30_gate_review_local.py:254-423`). `validate_gate_review_receipt` re-verifies the attester signature, exact input/request/attempt bindings, model/effort/tools, empty stderr, and the exact verdict line (`v35_p30_phase0.py:99-178`). The custody policy is byte-pinned in the trust root with `localOnlyRoles: ["review-attester"]` and the exact Claude executable path/hash/version (`v35_p30_crypto.py:266-294`). The analysis lane additionally requires the review to start strictly after manifest and draft issuance, to be distinct from the protocol review artifacts, and confines the authorizer to rebinding only the three timestamp fields (`analyze_v35_p30_long_horizon.py:3436-3508`). A forged remote unsigned claim cannot pass.

**6. One-shot launch/signing boundaries — confirmed.** Every child-launching executor call requires a custody-signed launch permit whose payload binds protocol, source contract, request hash, authorization, token, and the exact executor process identity (PID, start ticks, boot ID); the permit is validated — together with the full registered source closure — before token consumption or `Popen` (`v35_p30_authorized_execution.py:228-265`, `:1254-1265`), and a missing permit raises before any consumption. The role entrypoint refuses any request that is not the scheduler's byte-exact next action (`run_v35_p30_role.py:1680-1681`), emits `SIGN_READY` only after the durable unsigned plan or post-exit receipt draft exists, and receives the private key only afterward through custody (`:1728-1904`). Non-executor signing writes an `O_EXCL` attempt marker before key exposure and any marker without its artifact is terminal, enforced independently in both the reservation path and the scheduler (`run_v35_p30_role.py:197-217`, `run_v35_p30_campaign.py:1350-1358`). The analysis capability is a fully sealed anonymous memfd on fixed FD 198 (`v35_p30_authorized_execution.py:387-434`), and the `Popen`-entered-but-unproven state explicitly forbids `PRE_CHILD` recovery (`:1373-1389`).

**7. Exact-stack determinism — confirmed.** The Phase 0 preflight runs two fresh isolated inference servers with distinct socket inodes and the exact production evaluator invocation — 4,096 common public games, production worker count, identical batching flags, and the full determinism environment (CUBLAS workspace config, TF32 disabled, PCI bus ordering, pinned GPU UUID) — recording argv/environment hashes for both attempts and raising an unreceipted integrity failure on any per-game or replay-hash inequality (`run_v35_p30_cuda_determinism.py:41-307`). The guardian independently recomputes per-game and replay-hash equality plus their commitments before signing Phase 0 readiness (`run_v35_p30_role.py:1348-1384`), and evaluation-time bitwise agreement is enforced twice more (pair receipt and analyzer recheck). A mismatch has no recovery class by construction.

## Residual observations (not launch blockers)

- **Working-tree drift is again present and remains self-blocking.** `plan.md`, `protocol.json`, the analyzer, scheduler, crypto module, and the regenerated 544-file source registry are modified or untracked relative to the last locked commit. This state cannot execute: the role entrypoint, the executor primitive, and the running analyzer each re-hash every registered file against the lock (and the analyzer its own path), and `verify_git_context` binds the lock to the external commit. Before launch, these bytes must be recommitted, the source lock and external git context regenerated, and the protocol re-frozen — exactly the pre-token freeze-and-rerun flow the plan prescribes; the bytes reviewed here are the freeze candidates.
- The gate-review receipt validator checks the executable binding's presence and the attester signature but relies on the local launcher (trusted custody) for the executable-pin comparison; the analysis lane pins it in the validator itself. Within the stated threat model this is acceptable, since only local custody can produce the signature.
- The gate-review lanes permit exactly one remote attempt each, which is stricter than the plan's pre-token rerun allowance for the protocol review; strictness here is fail-closed and consistent with the plan.

## Conclusion

Every prior review finding named in the charge is concretely addressed in the current bytes, with enforcement duplicated at the scheduling, signing, and analysis boundaries and verified here at the code sites rather than in the plan text alone. No P0 or P1 launch blocker remains.

VERDICT: ACCEPT
