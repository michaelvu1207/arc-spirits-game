I've completed the verification pass. All seven charged areas check out at their actual enforcement sites, not just in the plan text. Final review follows.

---

# Final pre-authorization review (v4): V35 P30 long-horizon campaign

**Scope:** `plan.md` (tracked, unmodified, matching the locked commit `45449ea`) plus the source-locked implementation it names, read in full or at their enforcement sites: `analyze_v35_p30_long_horizon.py` (all 4,120 lines), `audit_v32_generation.py` (realized-dose core), `run_v35_p30_campaign.py` (storage/duration guard), `confirmation-gates.proposed.json`, `power-calibration.proposed.json`, and `source-lock.json` (540 registered paths, external shallow git context with fsck/ancestry/tree/inventory/blob binding).

## Verification of the prior review findings

**1. Quantitative confirmation gates — confirmed.** `confirmation-gates.proposed.json` freezes every Phase 5 number the plan summarizes and they agree exactly: 8,192 fresh public games with one-sided z = 2.128045…, minimum wins 6,631 (round-30, Wilson ≥ 0.80) and 7,431 (round-35, ≥ 0.90), the 100,000-draw paired bootstrap (seed 35153501) with a +0.05 post-round-15 VP floor; 4,096 private games with 3,337/3,732 floors and 751-win minimums per 1,024-game late-state slice across the four named slices; +50 overall Elo lower bound, per-format nonnegativity, 0.02 seat spread; 0.40 exploit ceiling and −0.02 improvement bound; zero-tolerance fairness/systems counters; 28/40 versus Michael with ties as non-wins; and an explicit conjunctive decision rule. `validate_protocol` pins the file by SHA-256 (`a3aca14a…`) and verifies its on-disk presence and hash (`analyze_v35_p30_long_horizon.py:714-727`), so it is frozen before P30 outcomes.

**2. Divergence classification — confirmed.** The bounded recovery policy is byte-pinned as a frozen dict (`PRE_CHILD`/`RECEIPT_ONLY` only, one per logical action, guardian classification required, second failure/outcome exposure closes the campaign; `analyze_v35_p30_long_horizon.py:630-642`), and the scheduler imports the full recovery state machine (`validate_guardian_incident`, `validate_sealed_pre_child_draft`, per-kind eligibility) from `v35_p30_recovery`. Deterministic replay divergence has no recovery class anywhere: the guardian pair receipt must carry exactly `["EXACT_PER_GAME_MATCH", "EXACT_REPLAY_HASH_MATCH"]` with hashes recomputed over the canonical per-game arrays (`:2777-2823`), and the analyzer independently re-verifies `replay_indexed == primary_indexed` and replay-hash equality (`:4039-4043`). Training-quality outcomes cannot be relabelled: audits require zero stalls/malformed rows and finite calibrated telemetry deterministically, so a weak-but-valid endpoint stays a valid bad endpoint.

**3. Realized-dose telemetry — confirmed.** `audit_v32_generation.validate_reach30_credit` (`audit_v32_generation.py:70-149`) requires `soloReach30Applied`, `soloReach30RealizedDose`, and per-band applied/dose for exactly bands `1-8`, `9-18`, `19-30`; band applied counts must sum to the total, band doses must sum to the realized dose (1e-9), each band dose must equal applied × the frozen coefficient (scalar arms use the scalar in every band, the scheduled arm the frozen band coefficients), simultaneous scalar+scheduled dosing is rejected, treatments must have applied > 0 and control exactly 0. The analyzer re-checks the credit block against the arm contract at analysis time (`:1935-1951`). These live only in the sealed audit; the scheduler never parses them, matching the plan's manipulation-check-only framing.

**4. Duration/storage go-no-go — confirmed.** `run_v35_p30_campaign.py` freezes `DISK_FLOOR_BYTES = 6 GiB` and `EVALUATION_RESERVE_BYTES = 512 MiB` (`:66-67`); the reservation is a real `posix_fallocate` allocation with sparse-file detection via `st_blocks` (`:102-154`), exclusive-locked, permission-checked, fsynced, and released one action at a time by lowering the target. The 1,209,600-second campaign ceiling is byte-pinned in the protocol runtime contract (`analyze_v35_p30_long_horizon.py:621-628`), which the scheduler validates via the shared `validate_protocol`, making the no-go non-discretionary. The preflight start additionally hard-requires 8 GiB initial headroom before any preflight action.

**5. Local-only review provenance — confirmed.** The analyzer accepts the analysis authorization only after `validate_analysis_review_receipt` verifies a **review-attester-signed** receipt against a locally custodied key, and the request binding pins the exact Claude executable from the protocol-pinned `reviewRuntime`, the attester public key by path+hash, and the one-shot attempt/stdout/stderr ledger paths (`:3436-3480`). The review must start strictly after both the manifest and the unsigned draft were issued (`:3481-3488`), must be distinct from the protocol review artifacts (`:3494-3508`), and `validate_reviewed_analysis_authorization_rebinding` plus byte-exact draft reproduction (`:3380-3391`) confine the authorizer to rebinding only the three timestamp fields after review completion. A forged remote unsigned claim cannot pass.

**6. One-shot launch/signing boundaries — confirmed.** The analysis launch boundary requires a signed authorization, consumed token, launch intent with a launch permit, and committed child launch evidence chained within five minutes; the capability is a fully sealed anonymous memfd on a fixed FD, checked for all four seals, exact size, hash, and the absence of any duplicate descriptor (`:2857-2905`). The analyzer proves its own PID/namespaces/start-ticks/executable hash match the committed evidence and that its namespaces differ from the supervisor's (`:3101-3139`), so direct CLI invocation fails even with copied files. Output sealing is `O_EXCL` with a pre-existence check and directory fsync (`:4094-4113`); generation and evaluation chains verify one-shot tokens, consumed markers, unique token IDs, and role-request bindings end to end.

**7. Exact-stack determinism — confirmed.** Evaluation receipts pin the full evaluator argv, cwd, and environment byte-exactly for both attempts (`:2369-2418`); primary and replay must use distinct attempt IDs and sockets (`:3879-3884`), the replay must consume the primary's signed receipt as predecessor (`:3885-3889`), and bitwise agreement is enforced twice (guardian pair receipt and independent analyzer recomputation). A mismatch has no recovery class by construction. The Phase 0 bitwise parity smoke on physical GPU7 remains a signed acceptance-checklist gate, as the plan requires.

**Power artifact — verified against the plan.** Planning effects +0.04/+0.025/+0.10, 262,144 exact sign assignments, eligibility gates +0.03 / 13-of-18 / ≤2 below −0.01, simultaneous Monte-Carlo joint lower bound 0.8090690427248277 ≥ 0.80, and the 0.024 / 0.0 sensitivity diagnostics with explicit no-adequacy disclaimers all match. The analyzer recomputes the artifact byte-for-byte from the frozen calibrator rather than trusting the literal (`:3166-3176`).

## Residual observations (not launch blockers)

- **Working-tree drift is present and self-blocking.** `ml/analyze_v35_p30_long_horizon.py` and its test file are modified relative to the locked commit (the file is now 4,120 lines versus the 4,106 reviewed at v3). This state cannot execute: the running analyzer re-hashes every registered file against the lock and its own path against the registry (`:3253-3265`, `:3544-3545`), `verify_git_context` proves each locked blob exists in the fsck-verified external commit, and the executor validates all registered source hashes before any token consumption. Before launch the tree must be recommitted and re-frozen (or reverted) so that deployed bytes, lock, and commit coincide — exactly the pre-token review-rerun flow the plan prescribes, with the bytes reviewed here being the freeze candidates.
- The protocol review receipt cryptographically binds the plan and the review transcript; the binding of the review to the full source lock is via the jointly frozen protocol rather than inside the receipt itself. This is within the stated threat model (local custody is trusted) and the runtime hash closure prevents any divergent bytes from executing.
- The realized-dose telemetry remains attestation of the trainer's own log line, internally cross-checked and decision-inert, consistent with its manipulation-check-only role.

## Conclusion

Every prior finding named in the review charge is concretely addressed and independently verifiable in the frozen contracts and the fail-closed implementation, with enforcement duplicated at the scheduling and analysis boundaries. No P0 or P1 launch blocker remains.

VERDICT: ACCEPT
