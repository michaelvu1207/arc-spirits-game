The role runner and fault matrix complete the picture: non-executor signing commits an `O_EXCL` attempt marker binding the exact unsigned payload before any private key is exposed (an existing marker terminally closes the action), and the eleven-case fault matrix covers every failure class the plan enumerates, including the `posix_fallocate` real-block reserve/shrink/release proof. My review is complete.

---

# Final pre-authorization review — P30 long-horizon campaign (plan + source-locked enforcement closure)

## Scope covered

I read the plan and, of its directly named closure, fully reviewed `run_v35_p30_campaign.py`, `v35_p30_phase0.py`, `v35_p30_crypto.py`, `v35_p30_authorized_execution.py`, `run_v35_p30_cuda_determinism.py`, `run_v35_p30_gate_review_local.py`, `run_v35_p30_fault_injection.py`, the signing/permit core of `run_v35_p30_role.py`, the decision/validation logic of `analyze_v35_p30_long_horizon.py`, plus the imported `v35_p30_analysis_review.py` and the frozen `confirmation-gates.proposed.json`. The issuer builders, rehearsal runner, tests, and registry generator were reviewed indirectly through the byte-exact reproduction validators that consume their outputs (the scheduler and analyzer reject any issuer or rehearsal artifact that does not reproduce exactly from the source-locked builders).

## Prior findings — concretely addressed

1. **Quantitative confirmation gates.** `confirmation-gates.proposed.json` is frozen and hash-pinned in the protocol (`validate_protocol` rejects any drift), with every plan Phase 5 threshold machine-readable: 6,631/8,192 and 7,431/8,192 public wins, +0.05 bootstrap VP bound with fixed seed/draws, 3,337 and 3,732 private wins, 751-per-1,024-slice floors, Elo +50 / per-format ≥0 / 0.02 seat spread, exploitability bounds, zero-tolerance fairness and systems counters, and 28/40 versus Michael. The analyzer's own eligibility gates (Holm ≤0.05 across all six treatment×primary tests, true-win point ≥ +0.03, ≥13/18 positive replicates, ≤2/18 below −0.01, non-regression thresholds) match the frozen protocol byte-for-byte, and the power artifact must reproduce byte-identically from `build_power_calibration` before any outcome is read.

2. **Divergence classification.** The bounded `PRE_CHILD`/`RECEIPT_ONLY` state machine is frozen in the protocol, enforced in the scheduler (`ensure_recovery_execution_draft` closes on any non-provable state: consumed-without-draft, invalid draft, second attempt), and in the executor (Popen entry without a child handle is explicitly non-recoverable; `terminal_on_pre_child_failure` makes the sole PRE_CHILD rerun terminal). Replay divergence is an integrity failure everywhere it can appear: the CUDA preflight raises on any per-game or replay-hash mismatch, and pair-integrity receipts require exact equality that the analyzer independently recomputes. Analysis has no recovery path by construction.

3. **Realized-dose telemetry.** `validate_generation_dose_telemetry` requires each generation audit to carry applied reach-30 rows and realized coefficient dose overall and in bands 1–8, 9–18, 19–30, with dose = applied × coefficient per band, strictly zero for control and strictly positive for treatment arms. The analyzer surfaces it as `realizedDose` in the sealed analysis output only; no gate or selection rule consumes it.

4. **Duration/storage go-no-go.** The signed preflight must project ≤ 1,209,600 compute-seconds and ≥ 6 GiB final headroom or `require_preflight` fails. `_rolling_budget_guard` recomputes remaining bytes at every scheduler call from max(frozen projection, observed per-action maximum with the 25% factor), enforces the 6 GiB + 512 MiB + projected-remaining floor, and maintains a real `posix_fallocate` reservation (sparse reservations rejected via `st_blocks`), releasing exactly one action's allocation per launch and returning to zero after the pairs complete. The rolling compute projection (`accumulated + duration_max × remaining`) stops issuance before the ceiling.

5. **Local-only review provenance.** The review-attester key is protocol-pinned local-only (`localOnlyRoles`, `privateKeyRemoteDelivery: False`); signing happens on the Mac via a stdin-piped `--sign-only` child. The v12 fetcher fix (absent target, scp-created, type+hash verified, then chmod/fsync) and v13 canonical request path (`gate_review_paths` and the scheduler status now bind identical `ledger/requests/<mode>-fable-review.json` bytes) are implemented. The v15 fixes are in place: authenticated Fable liveness and a ≤30 s cross-host clock probe run before the remote `O_EXCL` reservation; a durable completion record plus postprocess marker allow only hash-identical signing/upload resume with the remote attempt hash re-verified; Fable can never rerun in resume mode. The Docker contract explicitly forces `--security-opt seccomp=builtin` per container (correctly not trusting the daemon's `profile=unconfined` default), pins image digest, engine, daemon identity, zero capabilities, read-only root/capsule, no-log driver, and verifies forced container deletion; OAuth exists only on inherited fd 0. Unsigned review claims are rejected outright (`validate_analysis_review_draft` raises unconditionally).

6. **One-shot launch/signing boundaries.** Executor launches require a locally-custodied one-shot launch permit validated (with the full source-closure hash check and live process identity) before token consumption or `Popen`; the analysis path additionally uses a fully-sealed CLOEXEC memfd capability on the corrected bubblewrap `--sync-fd` spelling, with the analyzer verifying seals, single-FD uniqueness, namespace distinctness, and executable hash before opening any input. Executor sealing commits a durable seal-attempt marker before the private key is touched; non-executor signing commits an `O_EXCL` attempt marker binding the exact unsigned payload first, and any pre-existing marker terminally closes the action. The direct executor CLI is disabled. The corrected empty-attempt-directory and no-`/proc/driver/nvidia` contracts from the incident amendments are both enforced in code.

7. **Exact-stack determinism.** The CUDA preflight runs two fresh servers (distinct socket inodes) with the production evaluator command, batch shapes, worker concurrency, and pinned determinism environment (TF32 disabled, `CUBLAS_WORKSPACE_CONFIG`, PCI bus order, hash seed), requires empty evaluator streams, and demands bitwise per-game and replay-hash equality; any mismatch raises rather than retries, and the readiness gate requires the `CUDA_PRIMARY_REPLAY_EXACT` diagnostic.

## Remaining observations (none launch-blocking)

- The rolling compute guard re-projects runtime only over remaining generations; evaluation-phase compute is covered by the frozen preflight projection and per-execution 14-day bounds but not re-projected at evaluation boundaries. Given evaluation is a small, fixed 108×4,096-game workload already inside the signed projection, this is P2 at most.
- The CPU fault matrix executes pinned unit tests rather than end-to-end runs. This matches the plan's design (only the analyzer rehearsal was upgraded to the end-to-end v2 contract, which `validate_analyzer_rehearsal_report` enforces deeply), so it is consistent, not a regression.
- The plan's "directly reviewable enforcement closure" list omits a few load-bearing imported modules (`v35_p30_recovery.py`, `v35_p30_analysis_review.py`, `run_v35_p30_analysis_review_local.py`, `v35_p30_key_custody.py`); all are nonetheless in the 550-file source registry and hash-locked, and the executor re-verifies the closure at launch, so this is a documentation nit only.

No P0 or P1 launch blocker remains in the reviewed plan and enforcement closure.

VERDICT: ACCEPT
