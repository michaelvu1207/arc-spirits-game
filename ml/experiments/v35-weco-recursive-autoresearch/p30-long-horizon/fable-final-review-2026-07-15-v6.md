All charged areas have now been verified at their current enforcement sites. Final review follows.

---

# Final pre-authorization review (v6): V35 P30 long-horizon campaign

**Scope.** The current working-tree bytes of `plan.md` and the enforcement closure it names in full: `run_v35_p30_campaign.py` (all 2,403 lines), `run_v35_p30_role.py` (all 1,924 lines), `v35_p30_authorized_execution.py` (all 2,453 lines), `v35_p30_phase0.py`, `v35_p30_crypto.py`, `run_v35_p30_gate_review_local.py`, `run_v35_p30_cuda_determinism.py`, `run_v35_p30_fault_injection.py`, `issue_v35_p30_preflight_authorization.py`, `test_v35_p30_phase0_gates.py`, plus `confirmation-gates.proposed.json` and the analyzer's frozen-contract, audit-gate, pair-integrity, and analysis-provenance regions. Several closure files (`run_v35_p30_campaign.py`, `run_v35_p30_role.py`, `v35_p30_crypto.py`, the analyzer, `plan.md`, `protocol.json`) changed after the v5 ACCEPT of 2026-07-15, so every charged finding was re-verified against current bytes rather than carried forward. `audit_v32_generation.py` and `v35_p30_analysis_review.py` are unmodified since their prior line-level verification, which stands for them.

## Verification of the prior review findings

**1. Quantitative confirmation gates — confirmed.** `confirmation-gates.proposed.json` (unmodified since the last lock) freezes every Phase 5 number the plan summarizes, in exact agreement: 8,192 fresh public games with Bonferroni z = 2.128045…, 6,631 round-30 and 7,431 round-35 win minimums (Wilson ≥ 0.80/0.90), the 100,000-draw paired bootstrap (seed 35153501) with the +0.05 post-round-15 VP floor; 4,096 private games with 3,337/3,732 floors and 751-win minimums per 1,024-game slice across the four named late-state slices; +50 overall Elo lower bound with per-format nonnegativity and 0.02 seat spread; 0.40 exploit ceiling and −0.02 improvement bound; zero-tolerance fairness/systems counters with the 100 ms/1.10× and 60 s/1.25× latency and 1.10× GPU-memory ratios; 28/40 versus Michael with ties as non-wins; and an explicit conjunctive decision rule. `validate_protocol` pins the file by SHA-256 (`a3aca14a…`) and re-verifies the on-disk hash on every load (`analyze_v35_p30_long_horizon.py:714-727`), so a post-freeze edit fails closed.

**2. Divergence classification — confirmed.** The bounded recovery policy is byte-pinned in the analyzer (`:630-642`): only `PRE_CHILD`/`RECEIPT_ONLY`, one recovery per logical action, guardian classification required, second failure and outcome exposure both terminal. The scheduler's state machine (`ensure_recovery_execution_draft`, `validate_recovery_state`, `next_execution_action`) admits exactly those classes with `attemptOrdinal` fixed at 1, terminally closes on a consumed token without a canonical draft, an invalid draft that entered `Popen`, or a repeated seal attempt, and any unsealed state on a non-eligible kind (including analysis) closes the lane (`run_v35_p30_campaign.py:529-600`). Deterministic replay divergence has no recovery class anywhere: the Phase 0 determinism tool raises an unreceipted integrity failure on any inequality, and the guardian pair receipt must carry exactly `["EXACT_PER_GAME_MATCH", "EXACT_REPLAY_HASH_MATCH"]` with recomputed per-game and replay-hash commitments (`analyze_v35_p30_long_horizon.py:2776-2823`). Training-quality failure cannot be relabelled: the KL/clip/calibration/stall/malformed trust gates are deterministic schema checks, so a weak-but-valid endpoint remains a valid bad endpoint.

**3. Realized-dose telemetry — confirmed.** The analyzer re-validates each generation audit's `soloReach30Credit` against the frozen arm contract — coefficient equality, `reach30Horizon` fixed, applied rows strictly positive for treatments and exactly zero for control, and the frozen band schedule (`:1935-1951`) — with the per-band applied/dose arithmetic enforced in the unmodified `audit_v32_generation.validate_reach30_credit`. These quantities live only in sealed audits; the scheduler's rolling guard reads only artifact byte counts and receipt timestamps, and the final barrier binds audits by hash without parsing dose fields, matching the plan's manipulation-check-only framing.

**4. Duration/storage go-no-go — confirmed.** `DISK_FLOOR_BYTES` (6 GiB) and `EVALUATION_RESERVE_BYTES` (512 MiB) are frozen constants (`run_v35_p30_campaign.py:79-80`); the reservation is a real `posix_fallocate` allocation with sparse-regression detection via `st_blocks`, exclusive-locked, permission-checked, and lowered one action at a time (`:115-167`, `:1909-1920`). The guardian preflight computes the 25%-buffered projection (`* 5 // 4`) plus the evaluation reserve and raises before signing unless both the disk floor and the protocol-pinned 1,209,600-second ceiling hold (`run_v35_p30_role.py:1254-1300`); the ceiling itself is byte-pinned in `validate_protocol` (`analyze_v35_p30_long_horizon.py:627`). `require_preflight` re-verifies the signed fields and current headroom on every scheduler call, and `_rolling_budget_guard` recomputes remaining storage and compute from the larger of the frozen estimate and the running maximum observed per-generation bytes/runtime, stopping non-discretionarily. The preflight start hard-requires 8 GiB initial headroom, and the reservation is released only after all 54 pair receipts are sealed.

**5. Local-only review provenance — confirmed.** All review lanes are launcher-local and attester-signed. The gate-review launcher pulls only hash bindings from the remote scheduler status, downloads into a fresh 0700 capsule with per-file hash verification, reserves a one-shot remote `O_EXCL` attempt before Fable runs, executes the resolved Claude binary under a sandbox profile with `Read` as the only tool, requires exit 0, empty stderr, and the last nonempty stdout line exactly `VERDICT: ACCEPT`, and signs through the local vault pipe so the review-attester key never reaches SimForge (`run_v35_p30_gate_review_local.py:254-423`). `validate_gate_review_receipt` independently re-verifies the attester signature, exact input/request/attempt bindings, the protocol-pinned Claude executable, model/effort/tools, the sorted environment-key allowlist, empty stderr, and the exact verdict line (`v35_p30_phase0.py:99-224`). Custody policy is byte-pinned with `localOnlyRoles: ["review-attester"]` and the exact executable path/hash/version (`v35_p30_crypto.py:266-294`). The analysis lane additionally requires the review to start strictly after manifest and draft issuance, to be path-distinct from the protocol review, and confines the authorizer to rebinding only the three timestamp fields (`analyze_v35_p30_long_horizon.py:3436-3508`). A forged remote unsigned claim cannot pass.

**6. One-shot launch/signing boundaries — confirmed.** Every child-launching executor call requires a custody-signed launch permit binding protocol, source contract, request hash, authorization, token, and the exact executor process identity (PID, start ticks, boot ID); the permit and the full registered source closure are validated before token consumption or `Popen`, and a missing permit raises first (`v35_p30_authorized_execution.py:100-148`, `:228-265`, `:1254-1265`). The role entrypoint refuses any request that is not the scheduler's byte-exact next action (`run_v35_p30_role.py:1680-1681`), emits `SIGN_READY` only after the durable unsigned plan or post-exit receipt draft exists, and receives the key only afterward. Non-executor signing writes an fsync-backed `O_EXCL` attempt marker before key exposure, and a marker without its artifact is terminal in both the reservation path and the scheduler (`run_v35_p30_role.py:197-217`, `run_v35_p30_campaign.py:1350-1358`). The analysis capability is a fully sealed anonymous memfd on fixed FD 198, analysis has no recovery class, and both the executor and issuer direct CLIs are disabled. `Popen`-entered-but-unproven state explicitly forbids `PRE_CHILD` recovery (`:1373-1389`, `:1440-1453`).

**7. Exact-stack determinism — confirmed.** The Phase 0 preflight runs two fresh isolated inference servers with verified-distinct socket inodes and the exact production evaluator invocation — 4,096 common public games, production workers, identical batching flags, and the full determinism environment (CUBLAS workspace, TF32 disabled, PCI bus ordering, pinned GPU UUID) — recording argv/environment hashes for both attempts and raising an unreceipted integrity failure on any per-game or replay-hash inequality (`run_v35_p30_cuda_determinism.py:41-307`). The guardian independently recomputes per-game and replay-hash equality plus their commitments before signing Phase 0 readiness (`run_v35_p30_role.py:1362-1384`), and bitwise agreement is enforced twice more at evaluation time (pair receipt, analyzer recheck). The eleven-case CPU fault matrix covers every case the plan prescribes, including `posix_fallocate` real-block reservation.

## Residual observations (not launch blockers)

- **Working-tree drift is present and remains self-blocking.** `plan.md`, `protocol.json`, the scheduler, role runner, crypto module, analyzer, and source registry are modified or untracked relative to the last locked commit. This state cannot execute: the role entrypoint, the executor primitive, and the analyzer each re-hash every registered file against the source lock, and `verify_git_context` binds the lock to the external commit. Before launch these bytes must be recommitted, the source lock and external Git context regenerated, and the protocol re-frozen — exactly the pre-token freeze-and-rerun flow the plan prescribes. The bytes reviewed here are the freeze candidates.
- The gate-review receipt validator checks the receipt's executable binding against the protocol pin and the attester signature, but trusts local custody for the byte-level comparison of the executable actually run; the analysis lane pins it in the validator. Acceptable within the stated threat model, since only local custody can produce the signature.
- `ANTHROPIC_API_KEY` may appear in the sandboxed review environment; this is inherent to invoking the Claude API, is confined to the launcher-controlled process, and exposes no campaign-reusable secret to reviewed content.
- The empty `fable-final-review-2026-07-15-v6.md`/`.stderr` files are this run's own capture targets, not evidence of a prior failed attempt.

## Conclusion

Every prior review finding named in the charge is concretely addressed in the current bytes, with enforcement duplicated at the scheduling, signing, and analysis boundaries and verified here at the code sites rather than in the plan text alone. No P0 or P1 launch blocker remains.

VERDICT: ACCEPT
