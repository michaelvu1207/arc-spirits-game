I've now independently verified every area named in the review charter by reading the source-locked implementation directly. Here is my final pre-authorization review.

---

## Final pre-authorization review — V35 P30 long-horizon campaign

I reviewed `plan.md` and the source-locked implementation it names, tracing each prior-review finding to concrete, fail-closed code. Scope covered: `v35_p30_crypto.py`, `v35_p30_phase0.py`, `v35_p30_authorized_execution.py` (full), `v35_p30_recovery.py`, `run_v35_p30_campaign.py` (full), `analyze_v35_p30_long_horizon.py` (protocol validation, input/receipt/replay validation, main), `run_v35_p30_gate_review_local.py`, `run_v35_p30_cuda_determinism.py`, `audit_v32_generation.py`, and the `confirmation-gates.proposed.json` contract.

**Quantitative confirmation gates.** `confirmation-gates.proposed.json` is fully numeric and conjunctive (8,192-game public family with 6,631/7,431 win floors and a +0.05 100k-draw bootstrap bound; 3,337/3,732 private floors with 751-win 1,024-game slices; Elo +50 lower bound with 0.02 seat spread; exploitability, fairness-zero, latency/regression, and 28/40 Michael gates, closing with "every threshold is conjunctive; no strong result waives another gate"). `validate_protocol` hash-pins the file (`a3aca14…`) and re-verifies it byte-for-byte, and pins every development-screen eligibility constant (Holm across the 6-test family, +0.03 true-win point gain, ≥13/18 positive replicates, ≤2/18 below −0.01, non-decision-bearing primary bootstrap, decision-bearing safety bootstrap, tie-break order).

**Divergence classification.** Deterministic divergence has no recovery lane. `v35_p30_recovery.py` implements only `PRE_CHILD`/`RECEIPT_ONLY` and only for `generation`/`evaluation-primary`/`evaluation-replay`; `require_recovery_eligible_kind` terminally closes anything else. The analyzer refuses any primary/replay difference (`replay_indexed != primary_indexed or replay_report.replayHashes != report.replayHashes` → error), and the CUDA smoke raises a non-retryable integrity failure on any per-game or replay-hash mismatch. Training-quality failure cannot be relabelled: trust-gate breaches fail the audit itself, and non-finite/NaN checkpoints are non-retryable.

**Realized-dose telemetry.** `validate_reach30_credit` records and cross-checks `soloReach30Applied`, `soloReach30AppliedByBand`, `soloReach30DoseByBand` over exactly the 1-8/9-18/19-30 bands and `soloReach30RealizedDose`, enforcing band-sum consistency, dose = applied × frozen per-band coefficient (1e-9 tol), treatment arms applying >0 rows, and control exactly 0. The quantities are sealed audit outputs and never reach the scheduler.

**Duration/storage go/no-go.** `require_preflight` rejects any projection over the frozen 1,209,600-second ceiling or under the 6 GiB floor; `ensure_preflight_start` requires 8 GiB initial headroom; `_rolling_budget_guard` recomputes remaining bytes/runtime from the max of the frozen preflight estimate and observed signed receipts, enforces `6 GiB + 512 MiB + projected remaining` before each launch, and `set_storage_reservation` maintains a real `posix_fallocate` reservation with sparse detection (`st_blocks`), one-action-at-a-time release, and zeroing after the pair phase.

**Local-only review provenance.** `run_v35_p30_gate_review_local.py` pipes the review-attester key from the 1Password vault into a local signer subprocess and never ships it to SimForge; it atomically reserves a remote `O_EXCL` attempt before invoking Fable, runs the hash-pinned Claude executable under a default-deny `sandbox-exec` profile with `Read` only, requires empty stderr and an exact final `VERDICT: ACCEPT`, and `validate_gate_review_receipt` independently recomputes the required inputs, argv shape, and environment-key set — a forged or input-substituted remote claim fails closed.

**One-shot launch/signing boundaries.** Executor launches require a custody-signed one-shot permit validated (with full source-closure re-hash and live process identity) before token consumption or `Popen`; consumed markers, normal/recovery seal-attempt markers (fsync-backed before any key touch), and non-executor signing-attempt markers each make a second attempt terminal. The analysis lane adds a sealed-memfd capability on a fixed inherited FD, validated with namespace evidence inside the analyzer before any report is opened; direct CLIs are disabled and analysis has no recovery path.

**Exact-stack determinism.** `run_v35_p30_cuda_determinism.py` reuses the production stack (`isolated_command`, `--window-ms 2 --max-batch 512`, 24 workers, `CUBLAS_WORKSPACE_CONFIG`/TF32-off/`PYTHONHASHSEED=0`/PCI-bus-order env, 4,096 games) across two fresh servers with distinct socket inodes and requires exact per-game and replay-hash equality, emitting `PRODUCTION_BATCH_AND_CONCURRENCY_MATCH` only on success.

The keyless scheduler (`next_action`) enforces the Phase-0 → three-arm gen-1 preflight → separately-reviewed full-campaign authorization → generation-major training → completeness barrier → paired evaluation → metric-free manifest → local analysis review → single sealed analyzer ordering, revalidating the full signed hash chain at every step and never inspecting an outcome metric.

**Non-blocking (P2) observations, unchanged from the prior accepted review:** (1) the gate-review launcher zeros the Claude auth bytearray only on the success path, so an exception before that point leaves the secret briefly in local process memory until GC; (2) the CUDA determinism preflight re-specifies the server/evaluator argv inline (sharing `isolated_command` with production) rather than importing it, so the two must be kept in sync — the source lock and Fable review catch drift. Neither affects launch safety or evidence integrity.

I found no remaining P0/P1 launch blocker: every ambiguous, consumed-but-unsealed, divergent, or over-budget state closes the campaign instance rather than advancing it, and no outcome metric is reachable before all gates and reviews complete.

VERDICT: ACCEPT
