No Write tool is available in this session, so I cannot persist a memory file; proceeding directly to the final review.

---

# Final pre-authorization review: V35 P30 long-horizon campaign

**Scope reviewed:** `plan.md` plus the source-locked implementation it names: `analyze_v35_p30_long_horizon.py` (all 4,106 lines), `audit_v35_p30_generation.py`, `audit_v32_generation.py`, `v35_p30_analysis_review.py`, `v35_p30_authorized_execution.py` (launch/permit/consumption path), `run_v35_p30_campaign.py` (full scheduler), and `confirmation-gates.proposed.json`.

## Findings on the required items

**1. Quantitative confirmation gates — addressed.** `confirmation-gates.proposed.json` exists, is machine-readable, and freezes every Phase 5 number the plan summarizes: 8,192 fresh public games with Bonferroni z = 2.128 and minimum wins 6,631 (round-30) / 7,431 (round-35), the 100,000-draw paired bootstrap with seed 35153501 and +0.05 floor; 4,096 private games with 3,337/3,732 floors and 1,024-game late-state slices (751 wins/slice); +50 overall Elo lower bound with per-format nonnegativity and 0.02 seat spread; the four-family exploitability gauntlet with 0.40 exploit ceiling and −0.02 improvement bound; zero-tolerance fairness and systems counters; and the 28/40 Michael gate with ties-as-non-wins. The decision rule is explicitly conjunctive. The analyzer's `validate_protocol` pins this file by SHA-256 (`a3aca14a…`) and fails closed if it is missing or altered, so the gates are frozen before P30 outcomes as claimed.

**2. Divergence classification — addressed.** The frozen bounded-recovery policy (`PRE_CHILD`/`RECEIPT_ONLY` only, one recovery per logical action, guardian classification required, second failure closes the campaign) is byte-pinned in `validate_protocol`. The scheduler's `ensure_recovery_execution_draft` materializes only *objectively provable* classes: a consumed token without a canonical draft, an invalid draft after `Popen` entry, or any recovery state on a non-eligible kind (including analysis) terminally closes the lane with explicit errors. The executor distinguishes entering `Popen` from receiving a child handle and forbids `PRE_CHILD` inference once the spawn call is entered. Deterministic replay divergence has no recovery class: the guardian pair receipt requires exactly `["EXACT_PER_GAME_MATCH", "EXACT_REPLAY_HASH_MATCH"]` with recomputed hashes over the canonical per-game arrays, and the analyzer independently re-verifies `replay_indexed == primary_indexed` and replay-hash equality. Training-quality failure is not relabelable: non-finite tensors fail the audit deterministically and cannot produce a valid draft for either recovery class.

**3. Realized-dose telemetry — addressed.** `audit_v32_generation.validate_reach30_credit` requires the trainer's credit line to report `soloReach30Applied`, `soloReach30RealizedDose`, and per-band applied/dose for exactly rounds 1-8, 9-18, and 19-30; it verifies band applied counts sum to the total, band doses sum to the realized dose (1e-9), each band dose equals applied × the frozen coefficient (scalar arms use the scalar in all bands; the scheduled arm uses the frozen band coefficients), treatment arms must have applied > 0, and control must be exactly 0. It also rejects simultaneous scalar+scheduled dosing. These quantities live only in the sealed audit; the scheduler never reads them, so they cannot influence scheduling or selection, matching the plan's manipulation-check framing.

**4. Duration/storage go-no-go — addressed.** `run_v35_p30_campaign.py` freezes `DISK_FLOOR_BYTES = 6 GiB` and `EVALUATION_RESERVE_BYTES = 512 MiB`. The guardian preflight receipt must carry `projectedTrainingBytesWith25PercentSafety`, `projectedCampaignComputeSeconds ≤ 1,209,600` (from the pinned protocol runtime), and `projectedFinalFreeBytes ≥ floor` — validation raises on violation, so the no-go is non-discretionary. `_rolling_budget_guard` recomputes at every boundary from the *larger* of the frozen preflight estimate and the running maximum signed per-generation bytes/runtime (×5//4 = the 25% factor), requires `free + reservation ≥ floor + reserve + projected remaining`, maintains a real `posix_fallocate` reservation (with sparse-file detection via `st_blocks`), releases exactly one action's allocation at a time, and returns the reservation to zero only after all pair receipts are sealed. The rolling compute projection (`accumulated + duration_max × remaining`) is also gated against the frozen ceiling. All quantities come from signed receipts and are outcome-blind.

**5. Local-only review provenance — addressed.** `v35_p30_analysis_review.py` accepts only a **review-attester-signed** receipt (`validate_analysis_review_draft` now hard-rejects unsigned receipts). The receipt pins the exact approved Claude executable by absolute path, SHA-256, and version string; requires `sandbox-exec` with default-deny, reads limited to the capsule plus minimal macOS runtime roots, writes limited to the capsule, sanitized environment with the OAuth token as the only secret; requires the one-shot `O_EXCL` attempt reservation to precede the review, the review to start after both the draft and manifest were issued, empty stderr, exit code 0, and a final nonempty line of exactly `VERDICT: ACCEPT`. The scheduler returns `analysis-review-attempt-consumed` (lane closed) if the attempt marker exists without a receipt, and pins the protocol's `reviewRuntime.claudeExecutable` to the module constant. The analyzer additionally verifies the review-attester request binding, that the analysis review artifacts are distinct from the protocol review, and that the authorizer rebound only the three timestamp fields after review completion (`validate_reviewed_analysis_authorization_rebinding` plus byte-exact draft reproduction).

**6. One-shot launch/signing boundaries — addressed.** The executor requires a locally-signed launch permit *before* token consumption or `Popen`; the permit binds protocol, source contract, request hash, authorization, token, and the exact executor process identity (pid, startTicks, bootId), so replay from another process or a copied file fails. `validate_frozen_working_source` re-verifies every registered source hash inside the executor itself and requires the launch-security closure files to be registered. Consumed markers, receipts, drafts, seal-attempt markers, and the storage reservation all use `O_EXCL`/exclusive-link semantics; non-executor signing attempts are one-shot (`nonexecutor_signing_attempt_path` closes the campaign on a second attempt). The analysis capability is a fully sealed anonymous memfd on a fixed FD, validated by the analyzer for seals, byte length, hash, and duplicate-descriptor absence, alongside namespace/executable-hash launch evidence that must chain from consumed token → intent → evidence within five minutes. Analysis deliberately has no recovery path, and the analyzer writes its output exclusively with a pre-existence check.

**7. Exact-stack determinism — addressed.** The per-generation environment manifest pins `CUDA_DEVICE_ORDER`, UUID-scoped `CUDA_VISIBLE_DEVICES`, `PYTHONHASHSEED=0`, and `CUBLAS_WORKSPACE_CONFIG=:4096:8`, with exactly one visible device and the pinned GPU7 UUID (`GPU-53f5…`). Evaluation receipts pin the full evaluator argv, cwd, and environment byte-exactly for both attempts; primary and replay must use distinct tokens, sockets, and attempt IDs, and bitwise agreement is enforced twice (guardian pair receipt and analyzer re-verification). A mismatch is unrecoverable by construction, matching the plan's "integrity failure, not retryable infrastructure" rule. The Phase 0 bitwise parity smoke and the recent parity-preflight incident closure remain procedural acceptance-checklist gates that must be signed before launch, as the plan requires.

## Residual observations (not launch blockers)

- The realized-dose telemetry is parsed from the candidate-produced `train.log`. It is internally cross-checked and decision-inert (sealed, never read by the scheduler), so falsification cannot influence selection — consistent with its manipulation-check-only role, but it is attestation of the trainer's own report, not independent measurement.
- The review sandbox retains outbound network for the API call, as the plan discloses; the deny-default filesystem profile and metric-free capsule bound the exfiltration surface to the reviewed inputs themselves.
- Several working-tree files are modified relative to HEAD; this is expected at the review-pending stage, and the source-lock verifier plus in-executor closure checks fail closed if the launched bytes ever differ from the committed, locked implementation.

## Conclusion

Every prior finding named in the review charge is concretely and verifiably addressed in the frozen contracts and the fail-closed implementation, with enforcement duplicated at both the scheduling and analysis boundaries. I found no remaining P0 or P1 launch blocker.

VERDICT: ACCEPT
