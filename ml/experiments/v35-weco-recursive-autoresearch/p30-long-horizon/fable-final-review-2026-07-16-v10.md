I have completed the review. I read the plan, all prior-review focus areas against the current working tree (including the three files modified since the v9 review: `plan.md`, `ml/run_v35_p30_cuda_determinism.py`, `ml/test_v35_p30_phase0_gates.py`), and the named enforcement-closure sources: the scheduler, role runner, Phase 0 contracts, crypto/custody primitives, authorized-execution primitive, determinism runner, gate-review launcher, fault matrix, registry generator, the audit core, and the hash-bound gates/power artifacts.

## Findings by focus area

**Quantitative confirmation gates — addressed.** `confirmation-gates.proposed.json` is frozen, registered in the source registry (`generate-v35-p30-source-registry.mjs:67`), and matches every Phase 5 number in the plan exactly: 8,192 fresh public seeds with ≥6,631 round-30 wins (Wilson ≥0.80), ≥7,431 round-35 wins (≥0.90), 100,000-draw paired bootstrap ≥ +0.05 at the Bonferroni-corrected 0.98333 confidence; private 3,337/3,732 floors with 751-wins-per-1,024-game slices across the four named slices; Elo +50 overall / ≥0 per format / 0.02 seat spread; exploitability +0.03/−0.01/0.40/−0.02; fairness zeros; latency and GPU ratios; Michael 28/40 with ties as non-wins and a separate pre-game Fable freeze; explicitly conjunctive decision rule. The analyzer-side screen is frozen in the hash-bound power artifact: 18 replicates, 262,144 exact sign assignments, family of six at familywise 0.05, +0.03 true-win point floor, ≥13/18 positive, ≤2/18 below −0.01, joint simultaneous Monte Carlo lower bound 0.8090690427248277 ≥ 0.80, with the 0.024/0.0 sensitivity curves marked diagnostic-only — all matching the plan's literals.

**Divergence classification — addressed.** `ensure_recovery_execution_draft` cross-checks the frozen `recoveryPolicy.eligibleKinds` against code constants and closes terminally on: consumed token without a canonical draft, an invalid draft (which proves `Popen` was entered, so `PRE_CHILD` cannot be inferred), unsealed state for non-recoverable kinds, and any attempt marker without its artifact. The `spawn_call_entered` boundary in `v35_p30_authorized_execution.py` (comment at lines 1375-1378) makes a raised `Popen` unrecoverable. A CUDA primary/replay mismatch raises inside a started child, producing an invalid draft — terminal, matching "integrity failure, not retryable." Training-quality failure cannot be relabelled: `RECEIPT_ONLY` requires a `valid: true` draft.

**Realized-dose telemetry — addressed.** `audit_v32_generation.validate_reach30_credit` (lines 70-149, registered via the top-level `ml/*.py` registry rule and named `generationAuditCore` in the analyzer's source map) requires `soloReach30Applied`, `soloReach30RealizedDose`, and applied/dose maps for exactly bands 1-8, 9-18, 19-30; verifies band doses equal applied × frozen coefficient to 1e-9; forbids mixing scalar and banded credit; requires applied > 0 under a treatment schedule and exactly 0 under control. These quantities seal into the generation audit; the scheduler's rolling budget guard reads only receipt bytes and timestamps, so they cannot influence scheduling or selection.

**Duration/storage go-no-go — addressed.** `_attest_preflight` computes the 25%-buffered training projection plus the 512 MiB evaluation reserve and raises before signing unless both the 6 GiB floor and the protocol-frozen `runtime.maxCampaignComputeSeconds` ceiling hold; `require_preflight` re-validates both non-discretionarily on every scheduler call. `_rolling_budget_guard` recomputes remaining need from the larger of the frozen estimate and the observed per-generation maximum, maintains a real `posix_fallocate` reservation (sparseness detected via `st_blocks`), releases exactly one action's allocation at a time, and stops before token consumption. The 11-case fault matrix includes the real-block reserve/shrink/release proof.

**Local-only review provenance — addressed.** The keyless scheduler only emits a review-required status. `run_v35_p30_gate_review_local.py` fetches hash-verified, metric-free inputs into a 0700 capsule, consumes the remote attempt marker with remote `O_EXCL` before invoking Fable, runs the resolved Claude executable under a default-deny `sandbox-exec` profile with `Read` as the only tool, requires exit 0, empty stderr, and an exact final-line `VERDICT: ACCEPT`, then signs with the review-attester key piped from the local vault into a local child process — never sent to SimForge. `validate_gate_review_receipt` pins the exact 13-element argv shape, the environment-key allowlist, and the protocol-pinned executable path/sha256/version, and re-derives the verdict from the stdout bytes; the updated test proves a tampered executable pin is rejected.

**One-shot launch/signing boundaries — addressed.** `_prepare_execution_receipt` enforces the exact order: launch permit required and validated (including the executor-side full source-closure check) → consumed-token marker written `O_EXCL` → `Popen`. The permit is process-identity-bound (pid/startTicks/uid/bootId), persisted `O_EXCL`, and re-verified against a freshly rebuilt expected payload, so replay, forgery, or a stale process fails closed. Every non-executor signing action writes a durable `O_EXCL` attempt marker binding the exact unsigned plan before any key exposure, and a marker without its artifact is terminal in both the scheduler and the role runner. Executor keys are released only after `SIGN_READY`, which requires a valid on-disk draft. The analysis path adds the sealed-memfd capability on FD 198, namespace child evidence, supervisor-owned stdout/stderr/exitCode outside the child-writable mount, and deliberately no recovery. The TTL rebinding validator proves byte-identity outside `issuedAtUtc`/`notBeforeUtc`/`expiresAtUtc` with a strictly post-review window.

**Exact-stack determinism — addressed.** The (modified) `run_v35_p30_cuda_determinism.py` runs the protocol's full 4,096 common public games at production worker count, batch shapes, binary wire, and sampling temperature through two fresh servers with distinct socket inodes on the pinned physical GPU7 UUID, under the frozen determinism environment (TF32 off, `CUBLAS_WORKSPACE_CONFIG`, `PYTHONHASHSEED=0`), and requires exact per-game and replay-hash equality. The guardian (`_attest_phase0_readiness`) independently re-reads the raw primary/replay reports, recomputes equality, and cross-checks the executor's commitments before signing readiness. A mismatch raises inside a started child and is therefore terminal, not retryable.

**Incident amendment — addressed.** The `e6d754a9` root cause is concretely fixed: `_prepare_attempt_output_dir` accepts a supervisor-precreated attempt directory only when it is a plain, empty, non-symlink directory, with all three regression tests present in the modified `test_v35_p30_phase0_gates.py`, and `_prepare_output_parents` unchanged. Fresh-instance repetition is structurally enforced because every Phase 0 artifact lives under the campaign-instance ledger namespace, and the incident instance is recorded as permanently closed.

## Non-blocking observations (P2)

- `require_preflight` rechecks headroom on every scheduler call as `available − full original projection ≥ 6 GiB`. Because the campaign's own retained artifacts reduce availability while the subtrahend stays fixed at the full projection, this check is strictly tighter than the plan's remaining-bytes rule (which `_rolling_budget_guard` implements correctly) and could stall a campaign whose initial slack is less than roughly twice the projection. It is fail-closed, outcome-blind, consumes nothing, and is recoverable by freeing unrelated space on the shared volume, so it is an operational-robustness note, not an integrity defect.
- The determinism comparison enforces `perGame` length of exactly 4,096 but no minimum length on `replayHashes`. Both attempts run the identical frozen evaluator with `--include-replay-hashes`, and per-game equality carries the primary evidence, so the exposure is theoretical.
- The prior review's P2 notes stand: the plan's "directly reviewable enforcement closure" list omits some load-bearing imported modules (all registered and enforced by the executor's closure check), and the review sandbox's `ANTHROPIC_API_KEY` is the plan-accepted sole network-facing dependency.

All prior review findings are concretely addressed in the current source-locked implementation with registered test coverage, including the post-incident corrections in the two modified implementation files. No P0 or P1 launch blocker remains.

VERDICT: ACCEPT
