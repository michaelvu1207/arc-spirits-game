# Final pre-authorization review — V35 P30 long-horizon campaign

I read the plan and the named enforcement closure: the scheduler (`run_v35_p30_campaign.py`, all 2,403 lines), the execution primitive (`v35_p30_authorized_execution.py`), `v35_p30_crypto.py`, `v35_p30_phase0.py`, the recovery contracts, the role runner, both modified authorization issuers, the CUDA determinism preflight, the fault-injection matrix, the analyzer rehearsal, the local gate-review launcher, the analyzer test fixture, the trusted core audit, the frozen protocol, and the frozen confirmation-gates contract. Findings against each prior review dimension:

**Quantitative confirmation gates — addressed.** `confirmation-gates.proposed.json` is frozen (`frozen-before-p30-outcomes`) and hash-bound in `protocol.proposed.json` (`confirmationGates.sha256`). Every number in plan Phase 5 matches the machine-readable contract exactly: 6,631/8,192 round-30 wins and 7,431 round-35 wins at Bonferroni-adjusted one-sided Wilson bounds, the 100,000-draw paired bootstrap ≥ +0.05 at 0.98333 confidence, 3,337/3,732 private floors with 751-win/1,024-game late-state slices, Elo +50 overall / ≥0 per format / 0.02 seat spread, +0.03 aggregate and −0.01 per-family exploitability with 0.40 and −0.02 exploit ceilings, zero-tolerance fairness counters, the 100 ms / 1.10× / 60 s / 1.25× / 1.10× systems limits, and 28/40 vs. Michael with ties as non-wins. The contract is explicitly conjunctive.

**Divergence classification — addressed.** Only `PRE_CHILD` and `RECEIPT_ONLY` exist, frozen identically in the protocol's `recoveryPolicy` and `v35_p30_recovery.RECOVERY_ELIGIBLE_KINDS`; the scheduler cross-checks the frozen list against the code constant. Diagnostic codes are a closed, metric-free enumeration. Preflight and analysis kinds have no recovery path — any unsealed state is terminal (`ensure_recovery_execution_draft`). An invalid draft that entered `Popen` is terminal; PRE_CHILD is only claimable after supervisor stream cleanup proves zero authorized outputs exist. A CUDA primary/replay mismatch raises inside the source-locked child, yielding an invalid preflight receipt with no recovery class — exactly "integrity failure, not retryable infrastructure." Training-quality failures cannot be relabelled: audit gates (non-finite tensors, KL/clip/ECE/Brier, seed-set equality) fail in the audit itself, not the supervisor boundary.

**Realized-dose telemetry — addressed.** `audit_v32_generation.validate_reach30_credit` requires `soloReach30Applied`, `soloReach30RealizedDose`, and `soloReach30AppliedByBand`/`soloReach30DoseByBand` over exactly the 1-8 / 9-18 / 19-30 bands, with band sums reconciling to totals, dose = applied-rows × the frozen per-band coefficient (matching `late-scheduled` bands `[[8,0.15],[18,0.3],[30,0.5]]`), treatment arms required to apply >0 rows and control exactly 0. It seals into the chained generation audit; the analyzer's selection path (per the named test fixture) does not consume these fields, matching "cannot affect scheduling or selection."

**Duration/storage go-no-go — addressed.** `require_preflight` rejects any receipt whose `projectedCampaignComputeSeconds` exceeds the protocol's frozen `maxCampaignComputeSeconds: 1209600` or whose projected final free bytes fall below the 6 GiB floor; the 512 MiB evaluation reserve and floor are code constants. `_rolling_budget_guard` runs on every scheduler call, recomputing from the larger of the frozen preflight estimate and the running maximum observed signed bytes/runtime with the 25% factor, and maintains a real `posix_fallocate` reservation (sparse-file detection via `st_blocks`, single-action release, zeroed only after all 54 pairs seal). The fault matrix includes the real-block reservation/shrink/release proof.

**Local-only review provenance — addressed.** `review-attester` is in `localOnlyRoles` with `privateKeyRemoteDelivery: false`; the gate-review launcher reads the key from 1Password via `op read` piped to a stdin-only signing subprocess, the key never reaching SimForge. A remote O_EXCL attempt marker is consumed before Fable runs. The receipt pins the exact Claude executable (path + sha256 + version, cross-checked against the protocol), sandbox profile hash, 13-element argv, Read-only tooling, empty stderr, and the final nonempty stdout line must be exactly `VERDICT: ACCEPT`; the remote scheduler re-verifies the attester signature against the protocol-pinned public key. The analysis-review path mirrors this with the analysis authorizer verifying attester provenance.

**One-shot launch/signing boundaries — addressed.** Executor launch permits are validated before token consumption and `Popen`, bound to the request hash, authorization, protocol, and exact executor process identity (PID, start ticks, boot ID) — replay or CLI-direct calls fail closed. Consumed markers, unsigned drafts, and normal/recovery/pre-child seal-attempt markers are all O_EXCL with `attemptOrdinal: 1`; a marker without its artifact terminally closes the action (`emit_request` and `SigningReservation.reserve` both enforce this before any key exposure). Analysis is strictly one-shot: sealed memfd capability at a fixed FD, intent/evidence exclusivity, and deliberately no recovery path.

**Exact-stack determinism — addressed.** The Phase-0 CUDA preflight runs two fresh servers through the exact production evaluator command with production games (4,096), workers, batch shape (`--max-batch 512 --window-ms 2`), pinned deterministic environment (CUBLAS workspace, PCI bus order, TF32 disabled, PYTHONHASHSEED), GPU7 by UUID, and requires exact per-game equality, exact replay-hash equality, and distinct fresh-server socket inodes. Both launch incidents are concretely fixed: `_prepare_attempt_output_dir` accepts only a plain empty directory (symlink/non-dir/nonempty terminally invalid), and `validate_nested_controller_mount_contract` rejects any nested-isolation controller authorization carrying `/proc/driver/nvidia`, with both issuers omitting that mount. The venv launcher path is preserved unresolved while interpreter bytes are hashed separately (`venv_python_entrypoint` / `executable_sha256`).

The protocol is correctly non-executable in its current state (`authorized: false`, null campaign instance, null trust roots), and the named test proves `validate_protocol(require_authorized=True)` rejects it — a fresh instance must regenerate the full closure as the amendments require.

**Minor, non-blocking observations (P2/P3):** (1) `run_v35_p30_evaluation_attempt.py` and the two audit modules carry load-bearing contracts but are not listed in the plan's "directly reviewable enforcement closure" prose, though they are in the 545-file source registry the executor re-validates. (2) I could not independently recompute the `confirmationGates` sha256 binding in this session; the binding is validated fail-closed at runtime. (3) The 4,096 selection seeds sit at the start of the same declared public container as the future confirmation zone; disjointness of the later 8,192 fresh seeds is asserted by the gate contract and enforced by the separate Phase-5 protocol, and `seedEvidence.globalCompletenessProven: false` is candidly declared.

No P0 or P1 launch blocker remains.

VERDICT: ACCEPT
