I've completed the review. Here is the final pre-authorization review report.

# P30 Final Pre-Authorization Review (v22) — 2026-07-16

**Scope and method.** I reviewed `plan.md` in full and the source-locked enforcement closure it names, reading completely: `run_v35_p30_campaign.py` (2,435 lines), `v35_p30_phase0.py`, `run_v35_p30_cuda_determinism.py`, `run_v35_p30_gate_review_local.py`, `issue_v35_p30_preflight_authorization.py`, and the load-bearing majority of `analyze_v35_p30_long_horizon.py` and `v35_p30_authorized_execution.py`. The remaining named files (role runner, fault injection, evaluation/analysis issuers, rehearsal runner, tests, registry generator) were verified through the fail-closed validators that consume their outputs — every artifact they produce is re-validated byte-exactly by code I read directly, which bounds the impact of an unread producer defect.

## Findings by required dimension

**1. Quantitative confirmation gates — addressed.** `validate_protocol` hash-pins `confirmation-gates.proposed.json` (`a3aca14a…`) with `frozenBeforeP30Outcomes: true` and verifies file presence and hash. The analyzer's eligibility logic encodes every frozen number exactly: Holm at familywise α = 0.05 over the six-test primary family, 262,144 exact sign assignments, true-win point gain ≥ +0.03, ≥ 13/18 positive replicates, ≤ 2/18 below −0.01, the safety/non-regression thresholds (60,000 ms absolute p95, 1.25× matched-control ratio, zero stalls/malformed, guardian worst-case ≥ −0.05), and the deterministic tie-break (true-win point, then late-game-score point, then frozen arm order). The power artifact is not trusted as a literal: `load_inputs` recomputes it byte-for-byte via `build_power_calibration` and separately checks the Wilson/Bonferroni bound structure, the ≥ 0.80 joint-power requirement, and that the artifact's eligibility-gate parameters equal the protocol's analysis contract.

**2. Divergence classification — addressed.** Pair-integrity receipts require exact per-game and replay-hash equality with only the fixed diagnostics `EXACT_PER_GAME_MATCH`/`EXACT_REPLAY_HASH_MATCH`; a mismatch cannot produce a valid receipt and is not in the frozen recovery classes (`PRE_CHILD`, `RECEIPT_ONLY` only, checked against the protocol's frozen `recoveryPolicy`). `ensure_recovery_execution_draft` closes the lane terminally on any ambiguous state: an invalid unsigned receipt that entered `Popen`, a consumed token without a canonical draft, unsealed state on a non-eligible kind, or a second attempt (durable attempt markers make PRE_CHILD re-execution and RECEIPT_ONLY re-sealing hard failures). Analysis has no recovery path at all.

**3. Realized-dose telemetry — addressed.** `validate_generation_dose_telemetry` requires every generation audit to carry `soloReach30Applied`, per-band applied counts and doses for exactly bands 1-8/9-18/19-30, and `soloReach30RealizedDose`, with arithmetic consistency enforced (band dose = applied rows × frozen coefficient; band doses sum to realized dose; positive iff a treatment arm). The telemetry surfaces only inside the sealed analysis output (`realizedDose` per replicate); nothing in the scheduler reads it.

**4. Duration/storage go-no-go — addressed.** The generation-one preflight receipt is machine-rejected unless `projectedCampaignComputeSeconds ≤ 1,209,600` and `projectedFinalFreeBytes ≥ 6 GiB`, with the 512 MiB evaluation reserve pinned. `_rolling_budget_guard` recomputes at every boundary from the max of the frozen projection and the running observed per-action maximum (with the 25% factor), stops before token issuance, and maintains a real `posix_fallocate` reservation that releases exactly one action at a time; `storage_reservation_bytes` detects sparse regression via `st_blocks`. The rolling compute projection uses accumulated plus worst-case-remaining and stops fail-closed.

**5. Local-only review provenance — addressed.** All three prior rejections are concretely fixed: the v12 fetcher requires an absent target, lets `scp` create it, verifies regular-file type and hash, then chmods 0400 and fsyncs file and parent; the v13 path split is gone (`gate_review_paths` returns the canonical `ledger/requests/<mode>-fable-review.json`, and scheduler status, launcher, and remote validator all bind those bytes); the v15 findings are implemented (authenticated fd-0 liveness probe with the exact pinned runtime before reservation, cross-host clock bound, durable postprocess marker written before signing, and a resume path that verifies hash-identical bytes and can never rerun Fable). The review-attester key is read from 1Password into a local signing subprocess via pipe; it never reaches SimForge. The remote validator recomputes the launcher SHA-256, the full container create/start/cleanup argv, the canonical container config, verdict-line exactness, and zero-byte stderr.

**6. One-shot launch/signing boundaries — addressed.** Every child launch requires a locally-signed permit bound to the exact request hash, authorization, protocol, and the live executor process identity (pid, start ticks, boot ID) — replay or CLI invocation fails before `Popen`. Permits are `O_EXCL`-reserved; full source-closure hashes are re-verified inside the executor before token consumption. Non-executor signing uses durable signing-attempt markers that terminally close on a marker without its artifact. The analysis capability is a fully sealed memfd (all four seals, offset reset) delivered via `--ro-bind-data`, and the analyzer proves the fixed read-only path, closed-then-reserved FD 198, exact hash, non-duplication, and distinct pid/user/network namespaces before opening any input.

**7. Exact-stack determinism — addressed.** The CUDA preflight pins the deterministic environment exactly (TF32 disabled both ways, `CUBLAS_WORKSPACE_CONFIG`, `PYTHONHASHSEED=0`, PCI-bus ordering, UUID-scoped visibility), uses the production evaluator command, batch shapes, and worker concurrency, runs two fresh servers with distinct socket inodes, and fails closed on any per-game or replay-hash inequality. The incident amendments are all in code: the supervisor-created attempt directory is accepted only when a plain empty directory; `cuda_socket_root` derives from the full campaign ID under `/dev/shm/p/` and validates both complete pathnames against the 107-byte limit; the `/proc/driver/nvidia` read-only mount is present for non-CUDA preflights but omitted for the nested-isolation CUDA controller; and the external shallow Git database is included in read-only mounts so the sandboxed analyzer can re-verify every registered blob.

## Residual observations (P2, non-blocking)

- The plan's "experiment root must not contain a physical `.git`" clause is enforced indirectly (the analyzer rejects `gitDir == REPO_ROOT/.git` and verifies every blob against the external database); I did not find a literal directory-absence assertion on the experiment root. Blob-level verification makes this cosmetic.
- The rolling compute budget derives elapsed time from receipt timestamps rather than an independent clock; the pre-reservation cross-host clock bound and the 24-hour authorization window bound the drift this could introduce.

No P0 or P1 launch blocker remains. The campaign is still correctly gated behind a fresh disposable host probe, a fresh campaign ID, and the complete Phase-0 sequence before any seed can be consumed.

VERDICT: ACCEPT
